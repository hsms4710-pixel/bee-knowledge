# store 模块

包含 1 个文件

## 接口/类型

- `NodeData` (13 字段: id, sessionId, currentGraph, graphs, graph... +8)
- `EdgeData` (13 字段: id, sessionId, currentGraph, graphs, graph... +8)
- `GraphData` (13 字段: id, sessionId, currentGraph, graphs, graph... +8)
- `ClarificationOption` (13 字段: id, sessionId, currentGraph, graphs, graph... +8)
- `ClarificationQuestion` (13 字段: id, sessionId, currentGraph, graphs, graph... +8)
- `BrainstormSession` (12 字段: sessionId, currentGraph, graphs, graph, return... +7)
- `LayerType` (0 字段: )

## 依赖

### 外部依赖

- `zustand`
